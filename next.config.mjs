/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // basePath: "",
  // unoptimized: true,
  images: {
    domains: ["ap-south-1.graphassets.com"],
  },
};

export default nextConfig;
