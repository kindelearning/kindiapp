/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // basePath: "",
  // unoptimized: true,
  images: {
    domains: ["ap-south-1.graphassets.com"],
    unoptimized: true
  },
};

export default nextConfig;
